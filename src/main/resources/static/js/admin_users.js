function confirmDelete() {
	const confirmed = confirm("⚠️ Are you sure you want to delete this user?");
	if (confirmed) {
		// Add actual delete logic here
		alert("User deleted successfully (example)");
	}
}